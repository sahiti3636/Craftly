"""Craftly C2 — integrations.

Everything outside the platform is simulated: marketplace listing, courier
booking, the AI phone call. Each simulation sits behind the same small
interface a real integration would implement, so replacing one is a change
inside one module.
"""
